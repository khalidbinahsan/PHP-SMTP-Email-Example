# PHP-SMTP-Email-Example
This is the simple mail sending example through SMTP server. Added the PHPMailer file on "src" folder.
Now just adjust your form field and SMTP access to the "sendMail.php" file.